# Gemini CLI Project Instructions

@AGENTS.md

## Gemini Notes
- Use `AGENTS.md` as the shared source of truth for project rules.
- Keep `GEMINI.md` minimal and avoid duplicating instructions that already live in `AGENTS.md`.
